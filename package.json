{
  "name": "js",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "esbuild index.js --bundle --watch --loader:.js=jsx --outfile=dist/index.js",
    "build": "esbuild index.js --bundle --loader:.js=jsx --outfile=dist/index.js"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@babel/cli": "^7.16.0",
    "esbuild": "^0.14.2"
  }
}
